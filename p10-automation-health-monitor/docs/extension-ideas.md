# Extension Ideas

- Replace sample data with a private Google Sheet export.
- Add a small web UI for uploading a CSV or JSON file.
- Connect the output to n8n, Make, Zapier, Airtable, Notion, or a CRM.
- Add approval buttons before any email, webhook, order update, or listing publish action.
- Store run history in SQLite for before/after comparisons.
- Add GitHub Actions tests after secrets and deployment rules are configured.
