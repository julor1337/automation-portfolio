# Example Output

Running:

```bash
python src/run_demo.py
```

creates:

- `outputs/expenses_summary.csv`
- `outputs/monthly_summary.md`

Each row includes:

- item ID
- label
- score
- status
- flags
- recommended human actions
- short summary

The generated files are ignored by git so the public repository stays clean.
